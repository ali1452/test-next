import React from 'react'
import { Spinner } from 'reactstrap'

const Loader = () => {
  return (
    <div>
        <Spinner>
         Loading...
        </Spinner>
    </div>
  )
}

export default Loader